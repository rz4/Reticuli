# Experiments

The empirical program: does an independent model reproduce a claim, how often,
and at what cost? Everything here is generated by `scripts/sweep.py` over
`scripts/probe.py`, and it is built to stay honest.

## What the sweep measures

Each **cell** is (series × layer): a producer regrows one rung's stratum in a
clean room supplied with the *committed* lower layers, then the gate runs. A
cell **lands** iff the redo hits the committed root **and** `audit` passes —
the verdicts are re-earned, jailed, from the produced bytes, never carried. The
cost ledger records what the redo spent.

Series in the grid (`python3 scripts/sweep.py` prints the plan and estimates):

- **byte-copy** — positive control; correct source, must land 6/6 at $0.
- **negative-stub** — negative control; `stub = None`, must land 0/6 at $0.
- **{haiku, sonnet, opus} × {oneshot, agentic}** — blind vs iterate-until-it-passes,
  budget → pro. Agentic is bounded by `RETICULI_AGENT_BUDGET` per layer.

## Honesty rules, enforced

1. **Every row carries `claim_root`** — the committed root of the layer at
   measurement time. Data is never silently mixed across a re-mint; a row whose
   `claim_root` differs from the current committed root is stale by construction.
2. **`landed` requires `audit`**, not just root equality — a carried verdict
   never counts as a landing (see the [soundness fix](../guide.md)).
3. **One sweep at a time** — `sweep.py` takes a lock; the concurrent double-run
   that once corrupted the data cannot recur.
4. **Producers fail loudly** — a session-limit or error reply is a bounce, never
   written as if it were code.
5. **Specimens are kept** — every landed room is exported to
   `specimens/<series>/<layer>.tar`, a full record anyone can `ret import` and
   `ret audit` independently. The `.tar`s are gitignored (they can be large and
   many); the profile `.jsonl` is the committed index, and specimens can be
   published as a dataset/release when a result is written up.

## Files

- `../reflection_profile.jsonl` — the live index, one row per cell (committed).
- `specimens/<series>/<layer>.tar` — the actual regrown code, re-auditable (gitignored).
- `pre-audit-roots/` — retired data measured against roots the audit fix re-minted;
  kept for the record, **not** for analysis.

## The boundary taxonomy (what freeze-dries, what regenerates, what is history)

Every file in the repo belongs to exactly one class, decided by one question —
*if these bytes changed, would it still be the same repo?*

- **Identity** (change should change identity): the rung checks, recipes,
  manifests, pinned verdicts, LICENSE, logo. This is the freeze-dried core.
- **Free** (bytes may vary; the repo stays itself): all package code, scripts,
  tests, README, docs, pyproject, CI. Regenerable by any producer that lands
  in the basin; the workshop rung's TEETH clause keeps a regenerated test
  suite honest (an empty suite cannot land).
- **Residue** (what happened, never identity): experiment rows, specimens,
  ledgers, traces, generated figures and their tooling (`lagrange.py`), the
  cube experiment (whose deliberately weak checker is the documented
  Goodhart exhibit), retired data — under `docs/experiments/` — and
  **thought residue**: the essays under `docs/notes/` (basin, impedance,
  landscape) and the white paper, which record thinking and must never be
  "regenerated." Link-checks treat both namespaces as declared, never
  required. The claimed documentation is README + docs/guide.md (docs_check):
  the hand-off surface, held to verb-complete, env-complete, link-sound.

**The promotion rule (D):** tests are where knowledge is *discovered*; checks
are where it is *ratified*. When a test encodes claim-critical knowledge (the
fabricated-M3 attacks, the jail contract), promote it into the owning rung's
check — a deliberate re-mint — and leave the test as the bench copy.

## Known limits (state these with any result)

- **Self-referential** — six layers of Reticuli reproducing itself. External
  validity needs a non-self specimen (a real outside project).
- **Claude-family only** until a working OpenAI key is supplied.
- **`audit` assumes gates are deterministic and hermetic.** A nondeterministic
  gate fails audit loudly (the right direction), but such a gate is not a valid
  claim; quarantine helps enforce hermeticity.
- **Independence is not proven** — audit shows *these bytes earn these verdicts*,
  not that they were produced without copying M1. That is attestation's job.
- Small n so far; a real result wants n ≥ 3 per cell.
